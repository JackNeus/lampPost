#This is what the config file should look like (with values, of course)

DEBUG=True
PORT=5001
BASE_URL=http://localhost:5001

SECRET_KEY=

DB_NAME=
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=

# If set to True, will initialize data to data in static/mock_data/data.json
USE_MOCK_DATA=
# If set to True, will unprotect all API endpoints. Should only be used if absolutely needed.
BYPASS_API_AUTH=
