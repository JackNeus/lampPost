To install dependencies for this project, use the following command:
pip install -r requirements.txt
If this does not work, try
pip3 install -r requirements.txt
or
sudo pip3 install -r requirements.txt
